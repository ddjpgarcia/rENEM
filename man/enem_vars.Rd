% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{enem_vars}
\alias{enem_vars}
\title{Buscar variables a través de todas las olas}
\usage{
enem_vars(pattern, years = NULL)
}
\arguments{
\item{pattern}{Cadena. Expresión regular a buscar en el nombre o la
etiqueta de la variable (insensible a mayúsculas).}

\item{years}{Entero opcional. Restringe la búsqueda a un subconjunto de
años.}
}
\value{
Un \code{data.frame} (subconjunto del inventario) con columnas
\code{anio}, \code{variable}, \code{etiqueta}, \code{pct_missing}, \code{n_categorias_unicas}.
}
\description{
Busca en el inventario de variables (nombre o etiqueta) de las 10 olas
de ENEM. Útil para encontrar en qué años existe una variable antes de
intentar armonizarla.
}
\details{
Dos variables ya vienen con el mismo nombre en las 10 olas sin necesidad
de armonización: el ponderador (\code{PONDERADOR}, \code{PONDFIN}) y la ocupación
codificada en ISCO-08 (\code{isco08_1_ES}, \code{isco08_2_ES}, \code{isco08_1_ENG},
\code{isco08_2_ENG}, con la excepción de 2021 que solo trae la variante \verb{_1}).
El resto de las variables (sexo, edad, geografía, fecha) cambian de
nombre entre olas y requieren el crosswalk de \code{\link[=enem_codebook]{enem_codebook()}}.
}
\examples{
\dontrun{
enem_vars("pond")
enem_vars("voto|partido", years = c(2018, 2021, 2024))
}
}
