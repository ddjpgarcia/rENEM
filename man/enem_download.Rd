% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{enem_download}
\alias{enem_download}
\title{Descargar datos completos de ENEM (todas las olas o algunas)}
\usage{
enem_download(
  year = NULL,
  format = c("duckdb", "dta", "csv"),
  overwrite = FALSE
)
}
\arguments{
\item{year}{Entero o vector de enteros opcional. Si \code{NULL} (default),
incluye las 10 olas.}

\item{format}{Uno de \code{"duckdb"} (default -- regresa la ruta al archivo
completo, para abrir tú mismo con \code{\link[=enem_connect]{enem_connect()}} o \code{\link[DBI:dbConnect]{DBI::dbConnect()}}),
\code{"dta"} o \code{"csv"} (exporta la(s) tabla(s) pedidas a archivos locales en
\code{tempdir()} y regresa sus rutas).}

\item{overwrite}{Lógico. Si \code{TRUE}, vuelve a descargar el archivo
completo aunque ya exista en caché (para refrescar una versión nueva
del Release).}
}
\value{
Invisiblemente, la ruta (o vector de rutas) de los archivos
resultantes.
}
\description{
Se asegura de que el archivo completo de datos esté disponible
localmente (lo descarga la primera vez, después usa la copia en caché),
y regresa los datos en el formato que pidas. El archivo que se
descarga siempre es el mismo (\code{enem_data.duckdb}, ~22 MB, las 10 olas
juntas) -- \code{year}/\code{format} solo controlan qué te regresa esta función a
partir de esa copia local, no cuánto se descarga por red.
}
\examples{
\dontrun{
enem_download() # asegura la copia local, regresa la ruta al .duckdb
enem_download(2024, format = "csv")
enem_download(format = "dta") # las 10 olas, un .dta por año
}
}
