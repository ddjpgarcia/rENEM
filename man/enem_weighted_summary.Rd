% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{enem_weighted_summary}
\alias{enem_weighted_summary}
\title{Agregado ponderado de una variable de ENEM}
\usage{
enem_weighted_summary(
  data,
  var,
  by = NULL,
  weight = c("PONDFIN", "PONDERADOR")
)
}
\arguments{
\item{data}{Un \code{data.frame} de ENEM con columna de ponderador.}

\item{var}{Cadena. Nombre de la variable a resumir.}

\item{by}{Cadena opcional. Nombre de la variable de agrupación (p. ej.
\code{"anio"} o \code{"region"}).}

\item{weight}{Ver \code{\link[=enem_svy]{enem_svy()}}.}
}
\value{
Un \code{data.frame} con la media/proporción ponderada y su error
estándar (vía \code{\link[survey:svyby]{survey::svyby()}} cuando \code{by} no es \code{NULL}, o
\code{\link[survey:surveysummary]{survey::svymean()}} si es \code{NULL}).
}
\description{
Calcula la media (o proporción, si \code{var} es categórica) ponderada de una
variable, opcionalmente desglosada por grupo -- por ejemplo, intención
de voto ponderada por partido y año.
}
\examples{
\dontrun{
con <- enem_connect()
datos_2024 <- DBI::dbGetQuery(con, "SELECT * FROM enem_2024")
DBI::dbDisconnect(con, shutdown = TRUE)
enem_weighted_summary(datos_2024, "EDAD", by = "EDO")
}
}
