% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R, R/data.R
\docType{data}
\name{enem_codebook}
\alias{enem_codebook}
\title{Codebook armonizado de la serie ENEM}
\format{
Un data frame con las columnas:
\describe{
\item{anio}{Año de la ola (1997-2024).}
\item{concepto}{Nombre armonizado del concepto (\code{sexo}, \code{edad},
\code{municipio}, \code{fecha}, \code{folio}, \code{aprobacion_pdte},
\code{aprobacion_pdte_binaria}).}
\item{var_original}{Nombre de la variable en el \code{.dta} original de esa
ola para ese concepto.}
\item{var_armonizada}{Nombre de la columna correspondiente en
\link{enem_panel}.}
\item{disponible}{Lógico. \code{FALSE} cuando esa ola no trae el concepto
(p. ej. no hay variable de municipio en 2000).}
\item{notas}{Advertencias relevantes (p. ej. que \code{folio} no es un
identificador único salvo en 2024).}
}
}
\source{
Construido en \code{data-raw/build_codebook.R} a partir de los 10
\code{.dta} originales del Estudio Nacional Electoral de México (ENEM).
}
\usage{
enem_codebook(year = NULL)

enem_codebook
}
\arguments{
\item{year}{Entero opcional. Si se especifica, regresa solo las filas de
esa ola.}
}
\value{
Un \code{data.frame} con columnas \code{anio}, \code{concepto}, \code{var_original},
\code{var_armonizada}, \code{disponible}, \code{notas}.
}
\description{
Regresa el crosswalk que mapea, por año, la variable original al nombre
armonizado usado por \code{\link[=enem_load]{enem_load()}} -- para las 5 variables que sí
cambian de nombre entre olas (sexo, edad, municipio, fecha, folio; el
ponderador y la ocupación ISCO-08 ya vienen con el mismo nombre en las
10 olas, ver \code{\link[=enem_vars]{enem_vars()}}). Revisa la columna \code{notas}: varias filas
están marcadas \code{disponible = FALSE} (p. ej. no hay variable de
municipio en 2000) o traen advertencias importantes (p. ej. \code{folio} NO
es un identificador único de respondiente salvo en 2024 -- se repite
entre filas en el resto de las olas).

El crosswalk que devuelve \code{\link[=enem_codebook]{enem_codebook()}}: mapea, por año, la variable
original de cada ola al nombre armonizado usado en \link{enem_panel}, para las
variables que sí cambian de nombre entre olas (sexo, edad, municipio,
fecha, folio, aprobación presidencial).
}
\examples{
\dontrun{
enem_codebook()
enem_codebook(2012)
}
}
\seealso{
\code{\link[=enem_codebook]{enem_codebook()}}, \link{enem_panel}
}
\keyword{datasets}
