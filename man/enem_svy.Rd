% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{enem_svy}
\alias{enem_svy}
\title{Construir un diseño muestral ponderado para datos de ENEM}
\usage{
enem_svy(data, weight = c("PONDFIN", "PONDERADOR"))
}
\arguments{
\item{data}{Un \code{data.frame} con los datos completos de una ola (de
\code{\link[=enem_download]{enem_download()}} / \code{\link[=enem_connect]{enem_connect()}}) que incluya la columna de
ponderador.}

\item{weight}{Cadena. Cuál ponderador usar: \code{"PONDFIN"} (default,
recomendado -- incluye ajustes de no respuesta) o \code{"PONDERADOR"}
(ponderador de diseño puro).}
}
\value{
Un objeto \code{survey.design} (ver \code{\link[survey:svydesign]{survey::svydesign()}}).
}
\description{
Envuelve \code{\link[survey:svydesign]{survey::svydesign()}} usando el ponderador estandarizado de
ENEM. Las 10 olas ya traen, con el mismo nombre, dos ponderadores:
\code{PONDERADOR} (ponderador de diseño) y \code{PONDFIN} (ponderador final, con
ajustes de no respuesta y postestratificación cuando aplica) -- por eso
esta función funciona igual sin importar de qué ola vienen los datos, a
diferencia de otras variables que sí cambian de nombre entre años (ver
\code{\link[=enem_codebook]{enem_codebook()}}).
}
\details{
No se documentan unidades primarias de muestreo (UPM) ni estratos
explícitos en los \code{.dta}, así que el diseño se construye como muestreo
aleatorio simple ponderado (\code{ids = ~1}). Si en el futuro se recupera la
sección electoral como UPM (ver \code{Pendientes} del reporte de datos),
actualiza esta función para pasarla a \code{ids}.

Esta función es para los datos \strong{completos} de una ola (de
\code{\link[=enem_download]{enem_download()}} / \code{\link[=enem_connect]{enem_connect()}}, con columnas \code{PONDERADOR}/
\code{PONDFIN}). El panel armonizado de \code{\link[=enem_load]{enem_load()}} usa otros nombres
(\code{peso_diseno}/\code{peso_final}) -- para agregados sobre el panel usa
\code{\link[=enem_trend]{enem_trend()}}, que ya sabe cuál ponderador usar.
}
\examples{
\dontrun{
con <- enem_connect()
datos_2024 <- DBI::dbGetQuery(con, "SELECT * FROM enem_2024")
DBI::dbDisconnect(con, shutdown = TRUE)
d <- enem_svy(datos_2024)
survey::svymean(~EDAD, d, na.rm = TRUE)
}
}
