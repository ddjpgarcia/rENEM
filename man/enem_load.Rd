% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{enem_load}
\alias{enem_load}
\title{Cargar el panel armonizado de ENEM}
\usage{
enem_load(years = NULL)
}
\arguments{
\item{years}{Entero o vector de enteros opcional. Si se especifica,
regresa solo esas olas del panel.}
}
\value{
Un \code{data.frame} largo, con una fila por entrevistado y una
columna \code{anio}.
}
\description{
Carga \code{enem_panel}, el panel armonizado pequeño que se distribuye con el
paquete: variables comunes a las 10 olas (\code{mujer}, \code{edad_grupo}, peso de
diseño y final, ocupación ISCO-08, aprobación presidencial, y
geografía/fecha/folio cuando existen -- ver \code{\link[=enem_codebook]{enem_codebook()}} para qué
está disponible en cada año y con qué advertencias). Para los datos
completos de una ola (todas sus variables originales), usa
\code{\link[=enem_download]{enem_download()}} / \code{\link[=enem_connect]{enem_connect()}}.
}
\section{Advertencias importantes}{

\code{mujer} y \code{edad_grupo} vienen de variables ya recodificadas en los
\code{.dta} originales (\verb{female<año>}, \verb{age<año>b}); sus etiquetas de valor
confirman la convención estándar (\code{mujer}: 0=hombre/1=mujer;
\code{edad_grupo}: 1-4, de menor a mayor).
\code{municipio_original} y \code{fecha_original} NO están armonizados entre años
(mismos códigos no son comparables entre olas) y pueden ser \code{NA} (no
existen en 2000 y en 1997/2000/2003/2006/2012 respectivamente).
\code{folio_original} NO es un identificador único de respondiente salvo en
2024 -- ver la columna \code{folio_es_id_unico}; para una llave única en las
10 olas usa \code{id_ola}/\code{id_panel} en su lugar (\code{id_panel} no es
longitudinal entre olas, solo garantiza unicidad dentro de cada una).
\code{pdte_acuerdo} (aprobación presidencial) es una escala de 4 puntos
orientada para que mayor valor = mayor aprobación (1=Muy en desacuerdo
... 4=Muy de acuerdo) -- ver \code{notas} en \code{\link[=enem_codebook]{enem_codebook()}} para el
detalle de por qué se invirtió respecto al código original; usa
\code{pdte_aprueba} para el colapso binario (1=aprueba, 0=desaprueba).
\code{ideologia_lr}, \code{eval_pan}, \code{eval_prd} y \code{eval_pri} son escalas 0-10
conservadas tal cual (sin invertir); cualquier código de no respuesta o
"no conoce lo suficiente" ya está recodificado a \code{NA} -- ver \code{notas} en
\code{\link[=enem_codebook]{enem_codebook()}} para el detalle exacto por año.
}

\examples{
\dontrun{
enem_load()
enem_load(years = c(1997, 2024))
}
}
